<Window x:Class="SkyrimAEBackup.BackupDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Backup AE Content" Height="640" Width="680"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="120"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" FontSize="15" FontWeight="Bold"
                   Text="Select content to backup"/>

        <WrapPanel Grid.Row="1" Margin="0,10,0,4">
            <Button Content="All AE Content" Padding="10,4" Margin="0,0,6,4"
                    Click="SelectAllAE_Click" ToolTip="Select all files matching Skyrim.ccc (AE bundle)"/>
            <Button Content="All Other CC" Padding="10,4" Margin="0,0,6,4"
                    Click="SelectAllCC_Click" ToolTip="Select all cc* files NOT in the AE bundle"/>
            <Button Content="All (AE + CC)" Padding="10,4" Margin="0,0,6,4"
                    Click="SelectAll_Click"/>
            <Button Content="None" Padding="10,4" Margin="0,0,6,4"
                    Click="SelectNone_Click"/>
        </WrapPanel>

        <TextBlock Grid.Row="2" x:Name="CountText" Margin="0,2,0,6" Foreground="Gray"/>

        <Border Grid.Row="3" BorderBrush="#CCCCCC" BorderThickness="1">
            <ListBox x:Name="FileList" BorderThickness="0" Padding="4"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="2">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <CheckBox Grid.Column="0" IsChecked="{Binding IsSelected, Mode=TwoWay}"
                                      Checked="ItemCheck_Changed" Unchecked="ItemCheck_Changed"
                                      VerticalAlignment="Center"/>
                            <TextBlock Grid.Column="1" Text="{Binding Name}" Margin="6,0,0,0"
                                       VerticalAlignment="Center"/>
                            <Border Grid.Column="2" Background="{Binding BadgeBackground}"
                                    CornerRadius="3" Padding="6,1" VerticalAlignment="Center">
                                <TextBlock Text="{Binding CategoryLabel}" FontSize="10"
                                           Foreground="White" FontWeight="SemiBold"/>
                            </Border>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Border>

        <Grid Grid.Row="4" Margin="0,12,0,4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <ProgressBar Grid.Column="0" x:Name="Progress" Height="18" Minimum="0" Maximum="100"/>
            <TextBlock Grid.Column="1" x:Name="ProgressText" Margin="10,0,0,0"
                       VerticalAlignment="Center" Text="0 / 0"/>
        </Grid>

        <TextBlock Grid.Row="5" x:Name="StatusText" Foreground="Gray" FontSize="11"
                   Margin="0,2,0,4" Text="Ready"/>

        <Border Grid.Row="6" BorderBrush="#CCCCCC" BorderThickness="1" Background="#FAFAFA">
            <ScrollViewer x:Name="LogScroller" VerticalScrollBarVisibility="Auto">
                <TextBox x:Name="LogBox" IsReadOnly="True" BorderThickness="0"
                         Background="Transparent" TextWrapping="Wrap"
                         FontFamily="Consolas" FontSize="11" Padding="6"/>
            </ScrollViewer>
        </Border>

        <StackPanel Grid.Row="7" Orientation="Horizontal" HorizontalAlignment="Right"
                    Margin="0,12,0,0">
            <Button Content="Cancel" Padding="14,5" Margin="0,0,8,0"
                    Click="Cancel_Click" x:Name="CancelBtn" IsCancel="True"/>
            <Button Content="Backup" Padding="18,5"
                    Click="Backup_Click" x:Name="BackupBtn" IsDefault="True"/>
        </StackPanel>
    </Grid>
</Window>
